# DECISIONS — كل القرارات المعمارية (Decision Log)

ترتيب زمني تقريبي. كل قرار: **القرار → السبب**.

## أساسية
- **Git محلي فقط، بدون remote** → قرار المالك (خصوصية).
- **قراءة أولًا / آمن افتراضيًا** — كل كنكتور `read_only`، الكتابة `guard_write()`→403 → مايحصلش تعديل غير مقصود على أنظمة الشركة.
- **المفاتيح server-side بس** — المتصفح مايشوفش أي سرّ → ده جوهر سبب وجود الجيتواي (name-builder القديم كان بيسرّب مفاتيح في الـ JS).
- **بوابة مزدوجة (RBAC)** — أي إجراء بيتحكم فيه: صلاحيات الكنكتور + صلاحيات المستخدم الداخل.
- **البناء بالنمط** — 6 محرّكات قابلة لإعادة الاستخدام؛ ~180 متطلب بيترّكبوا عليها.
- **HTML+JSON لكل endpoint** عبر `respond()` — يشتغل من المتصفح والتطبيق والـ AI بنفس الرابط.
- **`BACKLOG.md` هو مصدر الحقيقة للأفكار، والكود بيقراه** (مش العكس) → المالك بيضيف سطر markdown، واللوحة تعرضه فورًا. لو الأفكار بقت 300 أو 500 مافيش تعديل كود.
- **سطر جديد في الباكلوج بيظهر من غير كود — بس مش من غير `wiring`** → اللوحة بتعرضه فورًا
  (بيرث `DOMAIN_DEFAULTS`)، لكن `test_graph.py` بيقع لحد ما ياخد سطر في `wiring.py` يقول
  ناقصه إيه بالظبط. مقصود: فكرة معروضة من غير "ناقص إيه" واضحة بتفضل قاعدة سنة، لأن محدش
  عارف يبدأ منين.
- **الفكرة المخطّطة ماينفعش تاخد لينك لحاجة مبنية** → اللينك بتاعها يفضل على كارتها؛ أقرب endpoint موجود بيتعرض كـ "أقرب حاجة موجودة" بس. عشان اللوحة ماتدّعيش شغل ماتعملش.
- **جاهزية الكنكتور بُعد مستقل عن الحالة** → "اتعمل" حاجة، و"نقدر نعمله دلوقتي" حاجة تانية. 107 فكرة كنكتورها جاهز = محتاجة تقرير بس، مش تكامل.
- **وصلة واحدة (`app/graph.py`) وكل الصفحات بتقطع منها** → كان في 4 خرايط مابتتقابلش (الأنظمة، الكنكتورات، الأفكار، اللوحة). لو كل صفحة حسبت لوحدها، الأرقام بتفترق — وده اللي حصل فعلًا (`/connectors` كان بيقول كنكتور واحد وإحنا عندنا ستة).
- **الـ endpoint اللي جنب الفكرة = داتا خام، مش تقرير** → الاسم نفسه هو الضمانة (`data_endpoints` مش `link`)، والاختبار بيمنع أي صفحة مبنية تتحط كمصدر داتا لفكرة مخطّطة. اللوحة ماتقدرش تدّعي شغل ماتعملش.
- **جدول الربط صريح (181 سطر) مش قاعدة** → ربط الفكرة بمصدر داتاها حُكم لكل فكرة (تقرير الشيكات بيقرا REST ولا SQL؟). القواعد لوحدها كانت هتحتاج ~230 استثناء، يعني أطول وأصعب. لكن أي سطر جديد بيرث دومينه فمابيقعش برّه الخريطة.
- **`catalog.py` = الجيتواي بيوصف نفسه، والاختبار بيقارن في الاتجاهين** → قبلها كان ينفع تضيف endpoint ومايظهرش على أي خريطة، أو الخريطة توعد بحاجة مش موجودة. الاتنين حصلوا.
- **النظام ≠ الدومين** → `SYSTEMS` = أطراف خارجية ليها credentials وlifecycle (نافذة، شركة الاتصالات، IdP). "مبيعات" و"موارد بشرية" دومينات بتتخدم بأنظمة، ومحلّها الباكلوج مش الخريطة. عشان كده اتضافوا 5 أنظمة بس مش 24.
- **الصوت العربي (STT) قدرة داخل `ai` مش نظام** → نفس نوع المورّد ونفس الـ credentials بتاعة الـ LLM؛ نظام منفصل كان هيفتّت طبقة الذكاء.
- **كوكي للمتصفح (GET بس)** → لينك `<a>` مايقدرش يبعت هيدر، فكل روابط الـ endpoints كانت هتموت 401 أول ما نحطّ مفتاح. الكوكي HttpOnly (يعني أخفى من المفتاح المحقون في الـ JS أصلًا) و**GET/HEAD فقط**، فحتى لو كنكتور اتفتح للكتابة، الكوكي مايقدرش يكتب.
- **الأرقام في الواجهة كلها محسوبة، مفيش رقم مكتوب بالإيد** → الهَب كان بيقول "5 كنكتورات" و"44 اختبار" وهما بقوا 9 و83، والجذر كان بيقول "11 نظام" وهما 22. أي رقم مكتوب بيبوظ بصمت.
- **الكتاب المرجعي (83 فصل) جوه الريبو** (`docs/enterprise-platform/`) → مصدر حقيقة واحد؛ مجلد منفصل بنفس اسم الملف كان بيضلّل أي شات جديد.

## نما (Nama)
- **Nama REST = CRUD كيانات فقط** (list/find/save/delete). اتأكد من عقد الـ OpenAPI نفسه. **مفيش أرصدة/تقارير/SQL في الـ API** → الأرصدة لازم مصدر تاني.
- **مصدر الأرصدة: SQL مباشر** (بدل انتظار نماسوفت) → أسرع وأنضف؛ REST مالوش لزمة للأرصدة. تقرير نماسوفت (`docs/nama-balance-report-spec.md`) بقى اختياري/مستقبلي.
- **login مخصص للجيتواي `stlix_gw` (read-only)** بدل استخدام `claude_ro` (بتاع الـ MCP) أو `sa` → مايكسرش الـ MCP، وأأمن من sa الأدمن.
- **الأرصدة من الفواتير (SalesInvoice/PurchaseInvoice.remaining)** مش من GL → GL مش مُرحّل بالكامل في الـ backup، لكن الفواتير مظبوطة.
- **أرصدة البنوك مؤجّلة** → محتاجة GL (غير مُرحّل بالكامل بالنسخة الحالية) — بنعرضها "قيد الترحيل" مش أرقام وهمية.
- **زيرو-نتيجة في نما = HTTP 400 body فاضي** (مش 200+[]) → اتعامل معاها `find_first`.
- **textCriteria operator = `Equal` بالظبط** (case-sensitive). التواريخ `DD-MM-YYYY`.
- **الأرصدة اللحظية تتبني من المستندات، مش من رصيد جاهز** → REST مابتنشرش أي رصيد محسوب،
  بس بتنشر الفاتورة وسطورها وسطور سدادها: `net = Σ details[].price.netValue`،
  `paid = Σ externalPaymentLines[].paymentValue`. اتقارن بـ SQL **مستند بمستند** على يونيو 2026:
  صفر فرق في الإجمالي، 1.49 ج تقريب في الصافي، وكل فرق في المحصّل مفسَّر بحركة بعد تاريخ النسخة.
- **الطرف في المستند = `subsidiaryId` مش `customer_id`/`supplier_id`** → `customer_id` فاضي في
  5,263 من 6,845 فاتورة مُرحَّلة، فالـ JOIN عليه كان بيخفي 19.8 من الـ 20.0 مليون مستحق ويخلّي
  المشكلة تبان «جودة بيانات». `subsidiaryId` بيطابق 100%. و`subsidiaryCode` **مايصلحش**
  (4,950 صف بكود قديم).
- **`maxRecords` نما بتتجاهله** → 5 و50 و1000 نفس الـ 25 صف. الـ paging الحقيقي `startPage` +
  `pageSize` (≤1000)، والصفحة تُعتبر أخيرة لما `returned + failed < pageSize` — **مش**
  `len(records)`، لأن السجل الفاشل بياخد مكان في الصفحة.
- **اللقطة اللحظية بتتبني في الخلفية، مش جوّه request** → السحب الكامل ~14,000 مستند / ~8 دقايق.
  صفحة بتستنى 8 دقايق أسوأ من صفحة بتقول «مافيش لقطة لسه». `POST /finance/live/refresh` بيبدأ
  ويرجع فورًا، و`GET /finance/live` بيقول عمر اللقطة وإيه اللي فات المسح.
- **الرقم بيشيل معاه اللي مقدرش يقراه** → في مستندات الـ credential مرفوض منها
  (`not accessible from this context`) ومافيش retry بيجيبها. بتتعدّ وأكوادها بتتنشر مع الرقم.
- **المطابقة سكربت، مش فقرة في CHANGELOG** → إن الـ live طابق SQL اتثبت مرة بالإيد.
  تأكيد ماتقدرش تعيده = ادّعاء مش ضابط. `scripts/reconcile_live_vs_sql.py` بيعيدها وقت ما
  تحب وبيرجّع exit 1 لو فيه فرق مش مفسَّر، فينفع يقف قدّام أي نشر.
- **وضعين للمطابقة، والسكربت مايخلطهمش** → REST المحلي وSQL بيقروا نفس القاعدة (فأي فرق =
  باگ في إعادة البناء، صفر تسامح)، لكن REST السحابي مقابل نسخة مُرستَرة الفرق فيه ممكن يكون
  سداد حصل بعد النسخة. لو الاتنين اتعاملوا بنفس المعيار، يا إما الصارم بيرمي حاجة صح، يا إما
  المتساهل بيعدّي باگ.
- **مافيش كود = غير مُرحَّل** → `@draft` مش العلامة الوحيدة. في القاعدة فاتورة مشتريات
  `Draft` من غير كود خالص كانت بتتحسب كمُرحَّلة. آمن نطلب كود، لأن **مافيش ولا واحد** من
  الـ13,686 مستند مُرحَّل كوده فاضي. المستند ده كان بـ0 ج — وعشان كده لازم قاعدة تمسكه، مش
  حد ياخد باله من مجموع غلط.
- **`subsidiary.code` من REST = الكود الحالي، مش المخزّن** → اتقاس على `SV00001`: SQL فيها
  `C00002` (مش موجود في `Customer`)، وREST رجّعت `C2000001`. يعني نما بتحلّ الـ id للكود
  الحالي، والتجميع على `subsidiary.code` في مسار الـ live **محصّن** من فخّ الـ226 كود القديم
  اللي كسر مسار SQL. المسار اللحظي هنا أأمن من العمود الخام.
- **مصدر الأرقام الافتراضي إعداد مش ثابت** (`FINANCE_DEFAULT_SOURCE`) → المالك عايز `live`
  افتراضي بعد ما المطابقة تعدّي. لو الافتراضي مكتوب في الكود، القلبة تبقى تعديل ونشر —
  والرجوع كمان. بقى سطر في `.env`، والرجوع نفس السطر.
- **الحارس بيفحص شهر مقفول، والثابت فيه هو المستندات مش الفلوس المحصّلة** → لو قارنّا
  الشهر الحالي، الفرق ممكن يكون تحصيل دخل قبل ساعة = إنذار كاذب كل ساعة. ولو اعتبرنا زيادة
  المحصّل انحراف، نفس الحكاية — النسخة المُرستَرة متأخرة في السداد بحكم التعريف. الثابت
  الحقيقي = **مجموعة المستندات + إجمالي وصافي كل واحد**. وده بالظبط اللي بيمسك التلات حالات:
  حقل اتغيّر (الصافي يقلّ) · صلاحية ضاقت (مستند يختفي) · مسودّة دخلت المُرحَّل (مستند يزيد).
- **شهرين لورا مش شهر واحد** → فاتورة بتاريخ آخر الشهر ممكن تتدخّل بعده بأيام، فـ«الشهر
  اللي فات» لسه بيتحرك. وأبعد من كده بيخلّي النافذة أقدم من غير ما يخلّيها أأمن.
- **`unknown` مايحوّلش الأرقام، `drift` بس** → إنك مش قادر تفحص (مافيش SQL، أول إقلاع،
  انقطاع) **مش** دليل إن الرقم غلط. التحويل على أساسه كان هيوقّف المالية لسبب مالوش علاقة بيها.
  وبالمقابل: الفشل في الفحص بيتقال `unknown` **أبدًا** مش `pass` — مافيش حاجة تستنتج إن
  الأرقام سليمة لأنها مقدرتش تبصّ عليها.
- **`drift` بيحوّل الافتراضي بس، و`?source=live` الصريح يفضل شغّال** → اللي بيطلب اللحظي
  بالاسم من حقّه ياخده بانحرافه — دي الطريقة اللي بيها تبصّ على اللي باظ. اللي بيتحمي هو
  الشخص اللي بيفتح صفحة من غير ما يختار مصدر.
- **رجوع السداد لورا لازم يتسمّى بالكود في الإعدادات** → السكربت كان بياخده وسيط سطر أوامر،
  والحارس الأوتوماتيك مالوش سطر أوامر. `FINANCE_ALLOWED_REVERSALS` — عشان الحارس مايتعلّمش
  يهزّ كتافه لسند ملغي.
- **قلب المقارنة في التطبيق، والسكربت بيستدعيه** → نسختين من «إيه اللي يعتبر فرق» بيختلفوا
  مع الوقت، واللي بيفرق منهم هو اللي محدش بيقراه. `reconcile.py` واحد للبوابة قبل النشر
  وللحارس في الإنتاج.
- **بيصدّر `sql` مش `live`** → الافتراضي بينزل على المصدر الكامل عمدًا. القلبة لازم تبقى
  فعل مقصود وراه مطابقة، مش افتراضي محدش فحصه.

## البيانات (Data)
- **live مش local** (طلب المالك) → النسخة المحلية القديمة (14 يوليو) مش كفاية؛ الحل نسخ يومية على Drive تترستور.
- **`.bak` لازم download+RESTORE** — مفيش قراءة للـ .bak وهو على الدرايف مباشرة → الأتمتة = سكربت ليلي.
- **الـ400 اللي فيها records مش خطأ** → نما بترجّع 400 على النجاح الجزئي؛ رميها كان بيضيّع
  تحصيلات ومدفوعات **حيّة** وصلتنا فعلًا. بنحتفظ بيها ونوسمها `_partial` — عشان الرقم الناقص
  يفضل مفيد، من غير ما يتنطق كأنه إجمالي.
- **عمر البيانات لازم يتقال** → «قديمة» حالة مقبولة، «قديمة ومتقدّمة كأنها لحظية» غش.
  صفحة التقارير كانت حاطة شارة «حيّ» على 11 يوم. دلوقتي `as_of` و`age_days` في كل رد مالي.
- **ماينزلش رقم فلوس قبل ما يطابق** → أي AR/AP محسوبة من REST لازم تتقارن بـ SQL لنفس الفترة
  الأول. إن البيانات قديمة سيّئ؛ إن رقم فلوس غلط أسوأ بكتير.

## Finance OS
- **العملاء من نما مش CRM** (طلب المالك) → كوكبيت مالي بيقرأ العملاء من الـ ERP، مش بيانات تواصل من CRM.
- **دورين جديدين: PRESIDENT (افتراضي، رؤية كاملة + قرار) + OPERATOR (إدخال، بدون اعتماد = فصل واجبات)**.
- **Finance OS = prototype** (labeled Demo) بأوفرلايز حقيقية؛ **الواجهة المالية الحقيقية = `/tools/finance-reports`**.

## المنصّة
- **هَب موحّد `/tools/platform`** = نقطة الدخول؛ يربط الموصّل ويعرض placeholders للمخطّط.
- **SSO placeholder** → التنفيذ الفعلي محتاج IdP (Google Workspace/OAuth) + قرار المالك.

## مؤجّل بقرار المالك
- 🔴 **تغيير المفاتيح المكشوفة** — أُجّل لحين استكمال التطوير (الكود نضيف؛ الناقص إلغاء القديم في الـ consoles).
