# CHANGELOG — كل تعديل

> ترتيب زمني تنازلي (الأحدث فوق). تواريخ تقريبية للجلسة (2026-07-24).

## 2026-07-25 (8) — النسخة النهائية: **كل شاشة على داتا لايف**، وفرع واحد
> طلب المالك: نسخة نهائية والكل شغال بالدات اللايف.

### اتصلح — الكوكبيت كان شايل فلوس مخترعة جنب فلوس حقيقية
`/tools/finance-os` كان نموذج أولي بداتا بتاعته: **٤ حسابات بنكية بأرصدة موهومة**
(4,823,000 · 962,000 · 210,000 · 1,315,000) · ٥ فواتير مشتريات · ٣ عملاء بكشوف حساب ·
٣ قيود يومية · ٥ توصيات ذكاء. لما الجيتواي كان بيرد، الأرقام الحقيقية كانت **بتتحط فوق جزء
منه** — والأقسام اللي مالهاش endpoint كانت بتعرض صفوفها المخترعة **والبانر مكتوب عليه
«متصل ✅»**. وده أسوأ من إن كله يكون مخترع: الفلوس الحقيقية جنب المخترعة هي اللي بتخلّي
المخترعة تبان حقيقية. وده كوكبيت حد بياخد فيه قرارات.

**اتعاد بناؤه — ٧ أقسام كل واحد من endpoint حيّ:**
عملاء (AR) · موردين (AP) · بنوك · فواتير مشتريات · قيود يومية · تحصيلات · مدفوعات.
وتاب **«مستني إيه»** فيه الخمسة اللي مالهمش داتا، وكل واحد بيقول ناقصه إيه بالظبط وبيوصّل
لصفحة النظام/الكنكتور: أرصدة البنوك (GL مش مُرحَّل) · الاعتماد (Layer 5) · التوصيات
(Layer 4) · RBAC (IdP) · ربط سجل التدقيق بالمستند.

**وقاعدة جديدة: المتصفح مابيجمّعش فلوس.** المستندات بتتعرض بهويتها بس (كود · تاريخ · طرف ·
عدد سطور). كل مجموع جاي من `/api/v1/finance/*` المتطابق مع SQL وشايل حُكم الحارس. حساب تاني
في الجافاسكريبت = إجابة تانية، واللي بتفرق منهم هي اللي محدش بيقراها.

### اتفضّى — `modules/finance-os/demo.html`
النموذج المستقل القديم (42 كيلو) بأرصدة وفواتير وكشوف مخترعة، **ومش موصّل بأي route**.
نفس فخّ `hub.public.html` بالظبط: صفحة يتيمة فيها فلوس، وأي إصلاح بعد كده مايوصلهاش. اتفضّت
وبقت تقول إنها كانت إيه وتوصّل للكوكبيت الحقيقي. الأصل في تاريخ git عند `986d43b`.

### اتضاف — اختبار بيقفل القاعدة (`tests/test_no_invented_numbers.py`)
6 اختبارات: مافيش رقم فلوس مكتوب في أي صفحة موصّلة · مافيش صفحة يتيمة تحت `modules/` شايلة
أرقام · كل قسم في الكوكبيت عنده `ep:` أو مسجّل كـ gap · مافيش مفاتيح الداتا القديمة ·
كل endpoint بيشاور عليه الكوكبيت موجود فعلًا في `app.openapi()`.
**واتحقق من الاختبار نفسه على الملف اللي استبدلناه** — بيقع عليه ويطلّع الأربع أرصدة
الموهومة والستّة مفاتيح. اختبار محدش جرّبه على الحالة اللي بيمنعها = ادّعاء تاني.
(الاستثناء المقصود: مساعد المهندس — دي آلة حسابية مش عرض لداتا الشركة، وأرقامها ثوابت
فيزيائية زي E=193000 ميجاباسكال.)

### اتصلح — الخريطة كانت بتغلط في الفعل
`/api/v1/finance/live/refresh` مكتوب في `catalog.py` كـ`GET` وهو `POST`. خريطة بتغلط في
الفعل زي خريطة بتغلط في المسار.

### اتوحّد — فرع واحد فيه كل حاجة
الشات التاني عمل commit على `main` (`36160fc`) وأنا كنت بابني فوق نفس التعديلات. اتأكد
بالمقارنة إن **مافيش حاجة في `main` مش موجودة عندنا** (الوحيد الفريد كان `document_money`
اللي انتقلت لـ`reconcile.py` وبتتصدّر من `live.py`، وتعليقين). فاتسجّل merge بيخلّي
`main` سلف لـ`live-numbers` — مش تجاهل شغل، بل امتناع عن إعادة تطبيق شغل موجود.
`main` يقدر يعمل fast-forward على الفرع ده في أي وقت.

### الاختبارات
**136 يعدّوا** (كانوا 130).

## 2026-07-25 (7) — حارس الانحراف: الرقم بقى يقول كمان إنه **كامل**، مش بس **حديث**
> عمر البيانات بقى ظاهر من كام جلسة. صحّتها مافيش حاجة كانت بتقولها.

### المشكلة بالظبط
المسح بينجح كل ساعة، بيرجّع رقم، والصفحة تعرضه بتوقيت حديث. تلات حاجات ممكن تتغيّر تحتينا
**من غير ما ترمي خطأ**:
- **نما تغيّر اسم حقل** (`maxRecords` طلعت مُتجاهَلة، و`fiscalPeriod` طلع مش عمود) — لو
  `netValue` اتغيّر، السطر يتقري **صفر** والمجموع يطلع **أقل**.
- **صلاحية الـ credential تضيق** — مستندات تبقى `not accessible` وتقع من المجموع.
- **قاعدة ترحيل جديدة** — مسودّة من غير `@draft` (زي اللي لقيناها من غير كود) تتجمّع مع
  المُرحَّل والمجموع يطلع **أكبر**.

في التلاتة الرقم يفضل معروض وهو ناقص، والشارة خضرا. رقم ناقص شكله زي أسبوع تحصيل حسن.

### الحل — وليه شهر **مقفول** تحديدًا
`app/integrations/finance/guard.py`: بعد كل مسح، شهر مقفول (شهرين لورا) بيتطابق تاني مع SQL.

**الثابت في الشهر المقفول** = **مجموعة المستندات** + **إجمالي وصافي** كل مستند.
**المحصّل مش ثابت** — فاتورة مايو بتتسدّد في يوليو، والنسخة المُرستَرة متأخرة في السداد بحكم
التعريف. فزيادة المحصّل = سداد، **مش انحراف**. عدم التماثل ده هو التصميم كله: لو اتعاملنا مع
السداد كانحراف، الحارس كان هيرنّ كل ساعة والناس تتعلّم تتجاهله.
- **شهرين لورا مش شهر:** فاتورة بتاريخ آخر يوم في الشهر ممكن تتدخّل بعده بأيام، فـ«الشهر
  اللي فات» لسه بيتحرك.
- **وشهر مش كل حاجة:** بضع مئات مستند مش 14,000 — الفحص لازم يبقى رخيص عشان يشتغل كل مرة،
  وإلا هيشتغل ولا مرة.

### السلوك
- `freshness.guard{status, reason, period, checked_at}` في **كل** رد مالي.
- التفاصيل مستند بمستند في `GET /api/v1/finance/live` · فحص فوري `POST /api/v1/finance/live/guard`.
- **`drift`** بيرجّع الافتراضي لـ`sql` **وبيقول إنه عمل كده وليه**. و`?source=live` الصريح
  بيفضل شغّال — دي الطريقة اللي بيها تبصّ على اللي باظ.
- **`unknown` مابيحوّلش.** مافيش SQL على الجهاز، أول إقلاع، انقطاع شبكة — إنك مش قادر تفحص
  **مش** دليل إن الرقم غلط، وتحويل الأرقام على أساسه كان هيوقّف المالية لسبب مالوش علاقة بالمالية.
- **الفشل بيتقال `unknown` أبدًا مش `pass`.** مافيش حاجة تستنتج إن الأرقام سليمة لأنها
  مقدرتش تبصّ عليها.
- الشارة ظاهرة على الهَب · الهَب العام · وصفحة التقارير، جنب عمر البيانات.

### اتغيّر — قلب المقارنة انتقل جوّه التطبيق
`app/integrations/finance/reconcile.py`. السكربت والحارس بيستدعوا **نفس** الكود. نسختين من
«إيه اللي يعتبر فرق» كانوا هيختلفوا مع الوقت، واللي يهم منهم هو اللي محدش بيقراه.
`document_money` انتقلت هناك و`live.py` بيعيد تصديرها، فكل import قديم لسه شغّال.

### الاختبارات
**130 يعدّوا** (كانوا 115). 15 اختبار جديد نصّهم على اللي **لازم يتمسك** (حقل اتغيّر · مستند
اختفى · مستند زيادة) ونصّهم على اللي **ماينفعش يرنّ** (سداد بعد النسخة · تقريب أقل من جنيه ·
حارس مش قادر يفحص).

## 2026-07-25 (6) — `live` بقى الافتراضي: الأرقام من **إنتاج** نما
> قرار المالك، بعد ما المطابقة الكاملة نزلت على **0.17 ج في AR و0.16 ج في AP**.

- `FINANCE_DEFAULT_SOURCE=live` في `.env`. `/api/v1/finance/{kpis,customers,suppliers}`
  بترد من مستندات **إنتاج نما** (`https://stlixvalley.namasoft.net/erp/rest/v1`) لما
  المستدعي مايقولش `?source=`. **الرجوع نفس السطر** — `sql` تاني، من غير تعديل كود.
- `LIVE_FINANCE_REFRESH_SECONDS=3600` — مسح خلفي كل ساعة بيبدأ عند الإقلاع، فمافيش زائر
  بيستنى الـ8 دقايق بعد أول مرة.
- **السلوك المقصود بعد كل restart:** لحد ما أول مسح يخلص، الصفحات المالية تقول «مافيش لقطة
  لسه» — **مش** بترجع لـ SQL. رقم مفقود واضح أحسن من رقم مصدره مش باين. (المالك اختار كده
  على الرجوع التلقائي.)
- SQL المُرستَر فضل المرجع الكامل على `?source=sql`، وعمره لسه ظاهر في كل رد.
- `hub.public.html` اتشال من البنود المفتوحة — اتوصّل خلاص.
- المهمة الجاية بقت **حارس الانحراف**: الأرقام بقت لحظية والمسح بيتكرّر كل ساعة من غير ما
  حد يتحقق منه تاني. تغيير حقل في نما أو ضيق في صلاحية الـ credential هيخلّي الرقم **ناقص
  وهو لسه معروض**. التفاصيل في `NEXT_STEP.md`.

## 2026-07-25 (5) — المطابقة بقت أمر تعيده · مستند كان بيتسرّب · والهَب العام اتوصّل
> الجلسة دي مالهاش رقم فلوس جديد. ليها **ضمانة** إن الأرقام اللي هتنزل تكون صح.

### اتضاف — `scripts/reconcile_live_vs_sql.py`
المقارنة اللي أثبتت إن الـ live مظبوط اتعملت **مرة واحدة بالإيد**. تأكيد ماتقدرش
تعيده = ادّعاء مش ضابط. بقى سكربت بيقارن مستند بمستند وبيرجّع **exit 1** لو فيه فرق
واحد مش مفسَّر:
- `--rest local` — REST المحلي وSQL بيقروا **نفس** القاعدة، فمافيش فرق مسموح خالص. ده الوضع الصارم.
- `--rest cloud` — REST السحابي مقابل نسخة مُرستَرة: **السداد بعد النسخة مفسَّر**؛ أي حركة
  في الإجمالي أو الصافي، أو سداد **بيرجع لورا** (سند اتلغى) = مش مفسَّر وبيوقف النشر.

### اتصلح — مستند غير مُرحَّل كان بيتحسب كمُرحَّل
`is_draft` كانت بتدوّر على `@draft` في الكود بس. في القاعدة المُرستَرة **4 مستندات غير
مُرحَّلة**: تلاتة عليهم `@draft`، وواحدة `PurchaseInvoice` **من غير كود خالص** (نما لسه
ماداتهاش رقم) — دي كانت بتعدّي كأنها مُرحَّلة. قيمتها **0 ج**، وعشان كده مكانش حد هيلاحظ؛
واللي بعدها ممكن ماتكونش. بقت أي مستند من غير كود = غير مُرحَّل، وده آمن: **مافيش ولا واحد**
من الـ13,686 مستند مُرحَّل كوده فاضي.

### اتقاس — الـ live محصّن من فخّ الأكواد القديمة
مسار SQL اتكسر قبل كده لأن `subsidiaryCode` بيحمل كود قديم في 4,950 صف، و**226 كود منهم
مش موجود في `Customer` أصلًا**. السؤال كان: الـ REST بترجّع القديم ولا الحالي؟ الجواب
اتقاس على `SV00001` — SQL فيها `C00002` (مش موجود)، وREST رجّعت **`C2000001`** الصح.
يعني نما بتحلّ `subsidiaryId` للكود الحالي، و`live.py` اللي بيجمّع على `subsidiary.code`
**مايقعش في الفخّ ده**. ✅

### اتغيّر — مصدر الأرقام الافتراضي بقى إعداد
`FINANCE_DEFAULT_SOURCE` (`sql` افتراضيًا). قرار المالك إن **live يبقى الافتراضي** بعد ما
المطابقة تعدّي — فبقى سطر واحد في `.env`، رايح وجاي، من غير تعديل كود.

### اتغيّر — `hub.public.html` بقى حيّ واتوصّل
كان **يتيم**: مافيش ولا سطر في المشروع بيشاور عليه، وفيه AR وAP مكتوبين بالإيد جنب تاريخ
مكتوب بالإيد — يعني بيقول موقف يوم واحد للأبد ومافيش حاجة فيه باينة غلط. بقى بيقرا من
`/api/v1/map` و`/api/v1/finance/kpis` (نفس اللي الهَب الأساسي بيقرا منه، فمستحيل يختلفوا)،
والروابط بقت نسبية بدل `http://localhost:8000`، واتوصّل على **`/tools/platform/public`**.

### اتضاف — من المالك
- **`LG9` — CargoX Document Creator & Validator** (فيه شات بوت): إنشاء مستندات الشحن
  والتحقق منها **قبل** الرفع على CargoX/ACID. مش زي `LG8`: داتا المستندات دي موجودة في نما
  فعلًا، فالناقص الطرف المستقبِل + Layer 4. → **183 متطلب**.
- **مساعد المهندس** اتسجّل كموديول مؤسسي في `VISION.md` جنب Finance OS وNameBuilder.

### اتوثّق — «ضيف سطر من غير كود» نصّها الدقيق
اللوحة فعلًا بتعرض أي سطر جديد فورًا. بس `test_graph.py` **بيقع** لحد ما الفكرة تاخد سطر
في `wiring.py` يقول ناقصها إيه بالظبط. الاختبار مقصود، والرَنبوك بقى بيقول كده صراحةً بدل
ما الشات اللي بعدنا يكتشفها بسويتة حمرا.

### اتحسّن — على إيد الشات التاني، قبل ما السكربت يتشغّل أصلًا
تلات باگات حقيقية في `reconcile_live_vs_sql.py`:
- **`WHERE fiscalPeriod = ?`** والعمود **مش موجود** في `SalesInvoice` (اتأكد من
  `INFORMATION_SCHEMA`: صفر) → كان هيموت من أول تشغيل. بقى `period_bounds()` على `issueDate`.
- **الأكواد المتكرّرة كانت بتتلغّي** في `dict[code]`، والمسودّة بتشارك المُرحَّل نفس الرقم →
  `_keyed()`.
- **رجوع السداد لورا** بقى محتاج `--allow-reversal <code>` صريح — سند ملغي لازم حد يبصّ له.

### الاختبارات
**115 يعدّوا** (منهم 10 على سكربت المطابقة نفسه؛ الرَنبوك كان لسه بيقول 89).

## 2026-07-25 (4) — الأرقام اللحظية: الإجابة **أيوه**، + رقم غلط كان معروض على الشاشة
> السؤال المفتوح كان: ينفع نحسب AR/AP لحظي من نما REST؟ الإجابة اتقاست مستند بمستند.

### الإجابة: أيوه — والمطابقة بالقرش
نما REST **مابتنشرش** أي رصيد محسوب، لكنها بتنشر المستندات. الرصيد بيتبني منها:
```
صافي الفاتورة  = SUM(details[].price.netValue)
المحصّل         = SUM(externalPaymentLines[].paymentValue)
المفتوح         = الصافي − المحصّل
```
اتقارن بـ SQL **مستند بمستند** على شهر يونيو 2026 (138 فاتورة بيع + 164 شراء):
- **الإجمالي قبل الضريبة: صفر فرق** في 302 فاتورة.
- **الصافي: فرقين مجموعهم 1.49 ج** (تقريب).
- **المحصّل: 20 فرق، كلهم مفسَّرين** — تحصيلات حصلت بعد تاريخ النسخة. الفرق الوحيد اللي
  اتحرك للورا (`FP22026060000105`, −230,500) هو سند صرف **اتلغى** في نما بعد النسخة —
  ونفس المستند طلع ناقص في مسح `PaymentVoucher` المستقل. مافيش فرق واحد من غير تفسير.

### اتصلح — رقم فلوس غلط كان معروض من زمان
- **`/api/v1/finance/customers` كان بيعرض 178 ألف من أصل 20.0 مليون مستحق.** السبب مش
  «جودة بيانات» زي ما الملاحظة القديمة كانت بتقول: الاستعلام كان بيعمل JOIN على
  `SalesInvoice.customer_id` — وده **NULL في 5,263 من 6,845 فاتورة مُرحَّلة**. الطرف
  الحقيقي في `subsidiaryId` (+ `subsidiaryEntityType`)، وبيطابق **100%** بدون فاقد.
  (`subsidiaryCode` مايصلحش: 4,950 صف بكود قديم مش موجود في `Customer.code`.)
- **المسودّات كانت داخلة في الـ KPIs** — دلوقتي `documentFileStatus='Stable'` في كل استعلام.
- **`remaining = netValue − totalPaid`** مش `total − totalPaid` (اتأكد على 6,829 من 6,848).
  الصفحة كانت بتعرض الإجمالي قبل الضريبة جنب رصيد محسوب على الصافي، فكان بيطلع عميل
  «مستحق عليه 176,705 وفواتيره 155,005». العمود بقى `invoicedNet` والسطر بيقفل.
- **AR مش كله على عملاء:** 1,016 فاتورة بيع مُصدَّرة على **موردين** و23 على موظفين =
  9.5 مليون من الـ 20.0. بقى فيه `arFromCustomers` و`apToSuppliers` جنب الإجمالي.

### اتصلح — كل نداء list كان محدود بـ 25 صف
`maxRecords` **نما بتتجاهله**: 5 و50 و1000 بيرجّعوا نفس الـ 25 صف. اللي بيشتغل
`startPage` (من 1) + `pageSize` (لحد 1000). اتضاف `list_page` و`list_all` بيلفّوا للآخر،
وبيوقفوا لما `returned + failed < pageSize` (مش `len(records)` — السجل الفاشل بياخد مكان
في الصفحة، فصفحة فيها 999 + 1 فاشل هي صفحة **كاملة**).

### اتضاف — `?source=live`
- `app/integrations/finance/live.py` — لقطة كاملة من مستندات نما الحيّة.
- `/api/v1/finance/{kpis,customers,suppliers}?source=live` · `/api/v1/finance/live`
  (حالة اللقطة) · `POST /api/v1/finance/live/refresh` (سحب في الخلفية).
- **مش بيتبني جوّه request:** السحب ~14,000 مستند / 14 صفحة / ~36 ثانية للصفحة = ~8 دقايق.
  صفحة بتستنى 8 دقايق أسوأ من صفحة بتقول «مافيش لقطة لحظية لسه».
- المستندات اللي الـ credential مش مسموح له يقراها (`not accessible from this context`)
  بتتعدّ وأكوادها بتتشال مع الرقم — مجموع من مجموعة ناقصة ماينفعش يبان كإجمالي حقيقي.


> طلب المالك: يتوصّل على النسخة الحيّة. الإجابة طلعت مركّبة، وفيها إصلاح مهم.

### اتصلح (الأهم)
- **الجيتواي كان بيرمي داتا حيّة وصلته فعلًا.** نما بترجّع HTTP 400 في حالتين مختلفتين:
  فلتر مالوش نتيجة (بودي فاضي)، **أو نجاح جزئي** (بعض السجلات فشلت والباقي جوّه الرد).
  `_handle()` كان بيرمي الاتنين → **`ReceiptVoucher` و`PaymentVoucher`** (تحصيلات العملاء
  ومدفوعات الموردين اللحظية) شكلهم مقفول وهما مش مقفولين. الرد الجزئي دلوقتي بيتوسم
  `_partial` عشان أي مجموع مايبانش كأنه إجمالي حقيقي.
- **صفحة التقارير كانت حاطة شارة «حيّ»** على أرقام عمرها 11 يوم. كل رد مالي دلوقتي فيه
  `freshness{as_of, age_days, live, stale}`، ومتعرضة على الهَب وعلى صفحة التقارير.

### اتأكد (قياس، مش افتراض)
- **نما REST حيّة أصلًا** — الموظفين والعملاء والفواتير كمستندات والبنوك والـ CRM والجرد.
- **SQL السحابي مقفول** — tcp/1433 و1434 على `stlixvalley.namasoft.net` الاتنين timeout.
  يعني مافيش طريق مباشر لأرصدة لحظية من SQL.
- `D:\hardsteel20260715.bak` موجود بس أحدث بيوم واحد بس عن المُرستَر — مايستاهلش.
- فخّين لأي حساب لحظي: **`records_count` بتاع الصفحة مش الإجمالي**، و**`@draft`** = مستند
  غير مُرحَّل لازم يتفلتر. الـ paging: `startPage` + `pageSize` (لحد 1000).

### أُضيف
- **موديول مساعد المهندس** `/tools/engineer` (`modules/engineer/`) — خزانات وأوعية بسُمك
  ASME UG-27 · مواسير صحية · ليزر وتشكيل · مبادلات · حصر BOM. اتحقق منه من على الجيتواي:
  خزان 1500×2000 → 4,418 لتر وسُمك 2.31 مم، مطابق لـ `PR/(SE−0.6P)` عند E=0.85.
  **أوفلاين** — مفيهوش مفاتيح ومابينده حاجة؛ الحصر لسه بيتنسخ بالإيد.
- دخل `BACKLOG.md` كـ **PD2** وظهر على اللوحة **بدون أي كود** → بقوا **182 متطلب**.
- `tests/test_nama_client.py` — بيثبّت دلالات الـ400 في الاتجاهين: الجزئي يتحفظ ويتوسم،
  والفاضي أو المُعلن فشله يفضل خطأ. **89 اختبار** (كانوا 83).

## 2026-07-25 (2) — جلسة: ربط الأفكار بالـ workspace وبالجيتواي (الهَب بقى مربوط فعلًا)
> طلب المالك: «اربط الأفكار بالـ workspace وبالـ gateway علشان يبقى hub — populate كامل متكامل».
> اللوحة كانت جزيرة: 181 متطلب معروضين ومش مربوطين بحاجة، و20 بند بس فيهم أي لينك.

### أُضيف
- **`app/ideas/wiring.py`** — مكان كل واحد من الـ181 في المنصّة: الأنظمة المسؤولة · **الـ endpoints اللي فيها داتاه الخام** · القسم في اللوحة · و"ناقص إيه" بجملة عربية. **135 من 181 داتاهم موجودة دلوقتي.** جدول صريح لكل id + `DOMAIN_DEFAULTS` عشان أي سطر جديد في الباكلوج مايقعش برّه الخريطة.
- **`app/catalog.py`** — الجيتواي بيوصف نفسه: 20 كنكتور + 54 endpoint (بما فيهم الـ501 placeholders، مشتقّة من `SYSTEMS` مش مكتوبة). الجاهزية بقت بتتحسب من `is_live()` هنا بدل نسخة تانية في `ideas/registry.py`.
- **`app/graph.py` + `GET /api/v1/map`** — الوصلة الواحدة (أنظمة × كنكتورات × endpoints × متطلبات × محرّكات). فلاتر `?system=` `?connector=` `?engine=` و`?view=`.
- **الاتجاه العكسي:** `/systems/{key}` · `/connectors/{key}` (اللي مستنيه بالظبط) · `/api/v1/ideas?system=|connector=|endpoint=|section=|has_data=`.
- **5 أنظمة** كان الباكلوج معتمد عليها ومش على الخريطة: `marketdata` · `portal` · `idp` · `omnichannel` · `telco`.
- **قسم `finance` في اللوحة الموحّدة** — مصدر الأرقام الحقيقية الوحيد كان ناقص من "اللوحة اللي بتجمّع كل حاجة".
- **`/tools/library`** — وثائق الريبو مقروءة من جوّه المنصّة (كان `BACKLOG.md` هو الملف الوحيد اللي المنصّة شايفاه).
- **كوكي `sg_key`** (HttpOnly · SameSite=lax · **GET/HEAD بس**) عشان لينك `<a>` عادي على `/api/v1/*` يشتغل.
- 27 اختبار جديد (`test_catalog.py` · `test_graph.py`) → **83 اختبار** (كانوا 56).

### اتغيّر
- **الهَب** — مافيش رقم مكتوب بالإيد: كل الأرقام من `/api/v1/map` + 26 كارت دومين بيوصّلوا للوحة مفلترة.
- **لوحة الأفكار** — الكارت بقى `<article>` مش لينك واحد: شارة النظام (فلتر) · الـ endpoints · "ناقص كنكتور" بيوصّل لصفحة الكنكتور · حالة الفلاتر كلها ظاهرة في الـ URL وقابلة للمسح فردي.
- **الـ501 placeholders** بقت بترد HTML/JSON وبتقول كام متطلب مستني النظام ده بدل رسالة خطأ جافة.
- **`/health`** بيقول حالة كل الكنكتورات الحيّة (كان بيفحص نما بس).
- **`/metrics`** فيه تجميع لكل كنكتور (كان paths خام مالهاش معنى).
- الباكلوج: 18 سطر اتوسموا بمحرّكهم — **Watchlist وMarket-Feeds كانوا صفر** رغم إن GV1 حرفيًا blacklist وMD1-MD5 حرفيًا market feeds.

### اتصلح
- **`/connectors` كان بيعرض `nama` بس** وستة كنكتورات حيّة — سطر مكتوب بالإيد من قبل الكنكتورات ما تتبنى.
- **5 كنكتورات بتشاور على أنظمة مش موجودة** على الخريطة (`idp`, `omnichannel`, `portal`, `telco`, `voice`) — وكانت بتتعرض في `/connectors` على طول.
- **17 route شغّالة ومش على أي خريطة** (الـ501 placeholders) + خريطة بتوعد بـ endpoints مش موجودة.
- أرقام بايظة: الهَب "5 كنكتورات"/"44 اختبار" (الحقيقة 9 و83) · الجذر "11 نظام" (22) · README بيقول `crm` و`inventory` مخطّطين وهما حيّين · VISION "4 live".
- `inventory` و`banks` كانوا بيقولوا **صفر فكرة** رغم إن 18 متطلب مخازن قاعدين فوقهم — الكنكتور دلوقتي بيتاخد من الـ endpoint اللي فعلًا فيه الداتا مش من نص عمود Source بس.

## 2026-07-25 — جلسة: لوحة الأفكار + توحيد المستودع
### أُضيف
- **سجل الأفكار** `app/ideas/` — بيقرا `BACKLOG.md` ويحوّله لبيانات: 181 متطلب، 26 دومين، مع **جاهزية الكنكتور** لكل بند (done/ready/partial/blocked) والمحرّك المرتبط.
- Endpoints `/api/v1/ideas` (فلاتر: `q`, `domain`, `status`, `readiness`, `engine`) · `/api/v1/ideas/domains` · `/api/v1/ideas/{id}`.
- **لوحة الأفكار** `modules/platform/ideas.html` + route `/tools/ideas` — كل فكرة كارت بلينك + deep link `#<id>`، بحث عربي/إنجليزي، فلاتر، عدّاد نتائج.
- `IdeasProvider` في اللوحة الموحّدة + كارت في الهَب بيعرض العدد الحيّ.
- 12 اختبار جديد (`tests/test_ideas.py`) → **56 اختبار** (كانوا 44).
### اتغيّر
- **نقل الكتاب المرجعي (83 فصل) جوه الريبو** → `docs/enterprise-platform/` (+ `README.md` يربطه بالتنفيذ) و `reference/finance-mvp/`. كان في `D:\CLAUDE SHARED MEMORY\Enterprise Platform AI based\` — بقى مصدر حقيقة واحد.
- توثيق التسمية: **الهَب = الجيتواي = المنصّة = مشروع واحد** (في الرَنبوك + `SESSION_STATE.json` + `HANDOFF.md`).
### اتصلح
- `_connectors_of` كانت بتقرا `ai` جوه كلمة `Email` (matching بالحروف) → بقت word-boundary.
- زرار "الكل" في لوحة الأفكار كان بيصفّر فلتر الحالة بس والدومين يفضل مقفول من غير ما يبان → بقى يصفّر كل الفلاتر + عدّاد نتائج ظاهر دايمًا.

## 2026-07-24 — جلسة: أرصدة حقيقية + منصّة موحّدة + handover
### أُضيف
- **SQL finance connector** (`app/integrations/finance/`) — يقرأ أرصدة العملاء/الموردين/KPIs من نما SQL مباشرة (pyodbc, read-only, `run_in_threadpool`). Endpoints `/api/v1/finance/{kpis,customers,suppliers}`.
- إعدادات SQL في `config.py` + `.env` (`NAMA_SQL_*`) + login `stlix_gw` (read-only) في SQL Server.
- `pyodbc` في requirements.
- **صفحة التقارير المالية** `modules/finance-os/reports.html` + route `/tools/finance-reports` — تسحب حيّ من `/api/v1/finance/*` (100% حقيقي).
- **هَب المنصّة** `modules/platform/hub.html` + route `/tools/platform` — نقطة دخول موحّدة، live modules + placeholders + SSO placeholder.
- Finance OS: شريط KPIs حقيقي في الداشبورد + أرصدة عملاء/موردين حقيقية في Customers 360 (من `/finance/*`).
- حزمة handover: `SESSION_STATE.json` · `MASTER_EXECUTION_RUNBOOK.md` · `TASKS.md` · `DECISIONS.md` · `CHANGELOG.md` · `NEXT_STEP.md`.
- `docs/nama-balance-report-spec.md` (مواصفة تقرير نماسوفت — اختياري) + `docs/live-balance-queries.sql`.
### اتغيّر
- Finance OS: العملاء من **نما** بدل CRM.
- الخزينة: شيلنا الأرقام الوهمية (الأرصدة الرقمية "قيد الترحيل من GL" — مش مُرحّل بالكامل في الـ backup).
### اتأكد (findings)
- Nama REST = CRUD كيانات فقط (OpenAPI). مفيش SQL/report/balance. أداة SQL في نما = web-login مش API.
- الأرصدة الحقيقية عبر SQL: sales 329.9M · AR 20.0M · purchases 320.0M · AP 186.7M (as-of 14 يوليو).
- data-quality: AR منسوب لعملاء بالاسم ~178K بس؛ الباقي فواتير غير مربوطة.
- نسخ backup يومية على Google Drive (`hardsteel<date>.bak`).

## قبلها (نفس الجلسة والجلسة السابقة)
### أُضيف
- **NameBuilder gateway-wired** (`demo.gateway.html` + `/tools/name-builder`): قوائم نما حيّة (`/nama/lists/{entity}`) + dedup (`/nama/invitem/exists`) + المحلّل المحلي — صفر مفاتيح في المتصفح.
- **UX1** في name-builder: بحث fuzzy (Levenshtein) + autocomplete منسدل بالكيبورد + تطبيع عربي.
- **Finance OS gateway-wired** (`/tools/finance-os`): أدوار PRESIDENT + OPERATOR الجديدة + RBAC + overlay بيانات حيّة.
- `NamaClient.list_query` + `find_first` (يتعامل مع zero-result 400) + endpoints `/nama/lists/{entity}` + `/nama/invitem/exists`.
- `app/routers/tools.py` (تقديم صفحات الموديولات مع حقن مفتاح الجيتواي).
- 5 كنكتورات (nama, attendance, crm, banks, inventory) + workspace + meta + observability + security + rate-limit.
### توثيق
- `BACKLOG.md` (~180 متطلب) + `VISION.md` (6 محرّكات) + `RESOURCES.md` + `templates/` + spec docs.
- بنود جديدة اتسجّلت: شكاوي · اقتراحات · transcript · صوت · autocomplete أصناف · payment slips · widgets · omnichannel · SSO · RBAC.
