# النشر — v1.0.0

> أول نسخة بتتعرض على ناس مابنوهاش. الفرق مش شكلي: الشاشة اللي بتقع قدّامك تفتكرها
> «هصلّحها بعدين»، واللي بتقع قدّام زميلك بتخلّيه ما يفتحش المنصّة تاني.

---

## أمر واحد

```powershell
cd "D:\Nama Code project\stlix-gateway"
.\deploy.ps1
```

بيعمل بالترتيب — وبيقف عند أول خطوة توقع:

| # | الخطوة | لو وقعت |
|---|--------|---------|
| ١ | البيئة والمكتبات (`.venv` + `pip`) | بيقف |
| ٢ | مفتاح الجيتواي — **لو فاضي بيولّد واحد ويحطه في `.env`** | بيكمّل ويقولك |
| ٣ | كل الاختبارات | **بيقف** |
| ٤ | فحص ما قبل العرض | بيقف لو فيه مانع |
| ٥ | التشغيل على `0.0.0.0` + طباعة عناوين الزملاء + فتح كروم | — |

```powershell
.\deploy.ps1 -LocalOnly    # على جهازك بس
.\deploy.ps1 -Port 8080
.\deploy.ps1 -SkipTests    # متعملهاش قبل عرض
```

---

## الزملاء بيفتحوا إيه

الاسكربت بيطبع عنوان كل كارت شبكة عندك. هما بيفتحوا:

```
http://<IP بتاعك>:8000/tools/tour
```

**ابدأ من `/tools/tour`** — دي صفحة الجولة: الفكرة، القواعد الأربعة، اللي شغّال، الأرقام
الحيّة، والذكاء — **وآخر قسم فيها بيقول اللي لسه مش شغّال**، مقروء من حالة المنصّة نفسها
مش من قايمة حد بيحدّثها. كل رقم فيها معاه لينك يثبته، فاللي مش مصدّق يدوس ويشوف الداتا
الخام بنفسه. جولة بتعرض الحلو بس بيتصدّقها عشر دقايق.

**مايحتاجوش يكتبوا مفتاح** — الصفحة بتدّي كوكي لأي حد بيفتحها. ده مقصود.

### الفايروول
لو مافتحش عندهم، ويندوز بيمنع البورت. من PowerShell **كأدمن**:

```powershell
New-NetFirewallRule -DisplayName "Stlix Gateway" -Direction Inbound `
  -Protocol TCP -LocalPort 8000 -Action Allow
```

### 🔴 شبكة الشركة بس
متعرّضش البورت ده على الإنترنت. مافيش SSO ولا صلاحيات لسه — أي حد يوصل للعنوان
بيشوف كل حاجة. ده مقبول لعرض داخلي، مش مقبول لغير كده.

---

## قبل العرض بربع ساعة

`FINANCE_DEFAULT_SOURCE=live` يعني الأرقام بتتبني من مستندات نما وقت التشغيل، والسحب
الكامل بياخد **دقايق**. لو شغّلت السيرفر وفتحت على طول، الصفحات المالية هتقول «مافيش
لقطة لسه» — وده صح، بس مش أحسن حاجة تبدأ بيها.

**شغّله قبل ما الناس تدخل بربع ساعة.** تقدر تتأكد من `/api/v1/finance/live`.

---

## الفحص لوحده

```powershell
.\.venv\Scripts\python.exe scripts\preflight.py
```

بيشتغل جوّه الـprocess — من غير سيرفر ومن غير شبكة ومن غير أي نداء لنما — فمينفعش
هو نفسه يبوّظ العرض. بيفرّق بين:

- **مانع** (خروج `1`): صفحة معلن عنها بتقع · الخريطة مش مطابقة للراوتس · كنكتور في وضع
  كتابة · مفتاح فاضي وإنت ناشر على الشبكة.
- **ملحوظة** (خروج `0`): مفيش موديل موصول · نصوص القوانين مش محمّلة. حاجات تقولها إنت
  قبل ما حد يسألك عنها.

---

## اللي هيكون ناقص في العرض (ولازم تقوله)

| الحاجة | الأثر | الحلّ |
|--------|-------|-------|
| `ANTHROPIC_API_KEY` مش متظبّط | الخبير والمستشار بيرجّعوا **المصادر** من غير صياغة | سطر في `.env` + restart |
| `corpus/legal/` فاضي | المستشار بيقول «القانون مش عندي» في كل سؤال | ارفع نصوص القوانين ثم `POST /api/v1/legal/reindex` |
| مافيش SSO / صلاحيات | أي حد على الشبكة بيشوف كل حاجة | PA2/PA3 — لسه |
| REP مش موصّل | ١١ وحدة مبنية بره الجيتواي | `docs/rep-integration.md` |

مافيش حاجة فيهم بتمنع العرض. كلهم ظاهرين في آخر قسم في `/tools/tour` — فلو ما قلتهمش،
الصفحة هتقولهم.

---

## Docker (بديل)

```bash
docker compose up -d --build
```

الـimage بينسخ `app` **و** كل اللي الكود بيقراه وقت التشغيل: `modules` · `docs` ·
`reference` · `corpus` · `BACKLOG.md`. كان بينسخ `app` بس — يبني ويشتغل و`/health` يردّ
٢٠٠، وبعدين **كل صفحة تقع**. `tests/test_deploy.py` بيقفل ده دلوقتي.

---

## الرجوع لورا

```powershell
git log --oneline -5
git checkout <commit>
```

الفرع `live-numbers` و`main` على نفس الكوميت. كل خطوة في التاريخ كانت الاختبارات
بتعدّي عندها.

---

## الأرقام

| | |
|---|---|
| الاختبارات | **213** |
| كنكتور حيّ | 12 |
| endpoint | 80 |
| شاشة | 12 |
| متطلب مسجّل | 214 |
| المطابقة المالية | 0.17 ج AR · 0.16 ج AP على كل المجتمع |
