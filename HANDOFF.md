# HANDOFF — حالة آخر جلسة (ابدأ من MASTER_EXECUTION_RUNBOOK.md)

> شات جديد؟ اقرأ `MASTER_EXECUTION_RUNBOOK.md` (المرجع) ثم `NEXT_STEP.md`. الملف ده لقطة آخر جلسة.
> كلّم المالك بالعربي المصري. `D:\Nama Code project\stlix-gateway` · git محلي · run: preview `stlix-gateway` (:8000).
> ⚠️ **الهَب = الجيتواي = المنصّة = المشروع ده** (كلام المالك). مافيش مجلد تاني.

## آخر جلسة عملت إيه (2026-07-25 — الجلسة التالتة)
**الطلب:** كمّل من `NEXT_STEP.md` — الأرقام اللحظية. التحقيق القديم (`wf_271db652`) مكانش
موصول، فاتعمل تحقق مستقل من الأول على نسخة نما المحلية (REST على :8080 + SQL على نفس
القاعدة — يعني نفس الداتا بالظبط، وده أقوى اختبار للمعادلة نفسها).

1. **المعادلة اتأكدت بالقرش** على مستندات حقيقية: `S2026060098` → REST رجّعت إجمالي 73,750
   وصافي 84,075 ومحصّل 1,026 ومفتوح 83,049 — **مطابقة تمامًا** لأربع أعمدة SQL. وسطر بقيمة
   صفر جه من غير مفتاح `netValue` أصلًا (نما بتشيل المفاتيح الصفرية) — `document_money`
   بتتحمّلها صح.
2. **🐛 مستند كان بيتسرّب:** `is_draft` بتدوّر على `@draft` بس. في `PurchaseInvoice`
   حالتها `Draft` و**كودها فاضي** كانت بتتحسب كمُرحَّلة. 0 ج، فمكانش حد هيلاحظ. اتصلحت.
3. **✅ الـ live محصّن من فخّ الأكواد القديمة:** `SV00001` كودها في SQL `C00002` (مش موجود
   في `Customer`) وREST رجّعت `C2000001` الصح — نما بتحلّ الـ id. التجميع على
   `subsidiary.code` سليم.
4. **`scripts/reconcile_live_vs_sql.py`** — المطابقة بقت أمر تعيده، بيرجّع exit 1 لو فيه
   فرق مش مفسَّر. وضعين: `--rest local` صارم · `--rest cloud` بيسمح بالسداد بعد النسخة بس.
5. **`FINANCE_DEFAULT_SOURCE`** — الافتراضي بقى إعداد. المالك قرّر **live يبقى الافتراضي**
   بعد ما المطابقة تعدّي؛ بينزل `sql` لحد ساعتها.
6. **`hub.public.html`** (قرار المالك: يقرا من `/api/v1/map`) — كان يتيم وفيه AR/AP وتاريخ
   بالإيد. بقى حيّ وعلى `/tools/platform/public`.
7. **من المالك:** `LG9` — **CargoX Document Creator & Validator** (فيه شات بوت) → 183 متطلب.
   و**مساعد المهندس** اتسجّل كموديول مؤسسي في `VISION.md`.
8. **105 اختبار يعدّي** (كانوا 102).

⚠️ **الخطوة الواحدة الفاضلة:** شغّل السكربت على جهازك، وبعدين اقلب `FINANCE_DEFAULT_SOURCE`.
التفاصيل في `NEXT_STEP.md`.

## الجلسة اللي قبلها (2026-07-25 — الجلسة التانية)
**الطلب:** «اربط الأفكار بالـ workspace وبالـ gateway علشان يبقى hub — populate كامل متكامل».
قبلها كانت لوحة الأفكار جزيرة: بتعرض 181 متطلب ومش مربوطة بحاجة. دلوقتي:

1. **`app/ideas/wiring.py`** — كل واحد من الـ**181** متطلب مربوط بـ: **النظام** المسؤول ·
   **الـ endpoints اللي فيها داتاه الخام** · **القسم** في اللوحة الموحّدة · و**"ناقص إيه"** بالعربي.
   **135 من 181 داتاهم الخام موجودة دلوقتي فعلًا.**
2. **`app/catalog.py`** — الجيتواي بقى بيوصف نفسه: 20 كنكتور + 54 endpoint. الجاهزية بقت
   بتتحسب منه بدل ما تكون مكتوبة مرتين. **`/connectors` كان بيعرض `nama` بس** وست كنكتورات حيّة!
3. **`app/graph.py` + `/api/v1/map`** — وصلة واحدة (أنظمة × كنكتورات × endpoints × متطلبات ×
   محرّكات)، وكل الصفحات بتقطع منها → **مستحيل رقم يختلف من صفحة لصفحة**.
4. **الاتجاه العكسي اتقفل:** `/systems/{key}` · `/connectors/{key}` (اللي مستنيه) ·
   `?ep=` (الأفكار اللي بتتغذّى من endpoint) · أقسام اللوحة بقى ليها روابط · وكل صفحة API
   بتقول فوقها هي فين من المنصّة.
5. **5 أنظمة جديدة على الخريطة** كان الباكلوج معتمد عليها ومش موجودة: `marketdata` ·
   `portal` (نافذة/الفاتورة الإلكترونية) · `idp` (SSO) · `omnichannel` · `telco`.
6. **الهَب بقى حيّ بالكامل** — مافيش رقم مكتوب بالإيد (كان بيقول "5 كنكتورات" و"44 اختبار"
   بعد ما بقوا 9 و83)، + 26 كارت دومين بيوصّلوا للوحة مفلترة.
7. **`/tools/library`** — وثائق المشروع نفسها بقت مقروءة من جوّه المنصّة (كان `BACKLOG.md`
   هو الملف الوحيد اللي المنصّة شايفاه).
8. **الكوكيز للمتصفح** — لينك عادي `<a>` على `/api/v1/*` كان هيرجّع 401 أول ما تحطّ
   `GATEWAY_API_KEY`. دلوقتي الصفحة بتاخد الكي كـ cookie **GET بس** (HttpOnly, SameSite=lax).
9. **83 اختبار يعدّي** (كانوا 56) — منهم `test_catalog.py` و`test_graph.py` اللي بيقفلوا
   القواعد: كل فكرة على نظام حقيقي · كل endpoint موجود فعلًا · **ولا فكرة مخطّطة بتاخد لينك
   لصفحة مبنية**.

## وبعدها في نفس الجلسة — «connect to live version»
10. **نما REST طلعت حيّة أصلًا** (موظفين · عملاء · فواتير كمستندات · بنوك · CRM · جرد).
    اللي مش حيّ هو **الفلوس بس** — من نسخة مُرستَرة. و**SQL السحابي مقفول** (1433/1434 timeout).
11. **الجيتواي كان بيرمي داتا حيّة وصلته:** نما بترجّع **400 على النجاح الجزئي** والكلاينت
    بيعتبرها خطأ → `ReceiptVoucher` (تحصيلات) و`PaymentVoucher` (مدفوعات) بقوا مقروئين.
    الرد الناقص بيتوسم `_partial` عشان أي مجموع مايبانش كأنه إجمالي حقيقي.
12. **عمر البيانات بقى ظاهر** — كل رد مالي فيه `as_of`/`age_days`/`stale`.
    (صفحة التقارير كانت حاطة شارة **«حيّ»** على أرقام عمرها 11 يوم.)
13. **مساعد المهندس** `/tools/engineer` (PD2) — خزانات/مواسير/ليزر/مبادلات + حصر BOM.
    اتحقق منه: خزان 1500×2000 → 4,418 لتر وسُمك 2.31 مم = ASME UG-27 عند E=0.85 بالظبط.
    دخل `BACKLOG.md` وظهر على اللوحة **بدون كود** → بقوا 182 متطلب.
14. **89 اختبار يعدّي.**

✅ **التحقيق ده اتقفل:** أيوه، ينفع. المعادلة اتحققت بالقرش، والفخّين اتمسكوا (وطلع
تالت: مستند من غير كود). الباقي = تشغيل سكربت المطابقة وقلب الافتراضي — `NEXT_STEP.md`.

## وقبلها (2026-07-25 — الأولى)
لوحة الأفكار `/tools/ideas` نفسها + بُعد جاهزية الكنكتور + توحيد المستودع (`docs/enterprise-platform/`).

## الحالة الحالية
- **الطبقات:** 1 Gateway ✅ · 2 Connectors (6 حيّة) · 3 Workspace ✅ (8 أقسام) · 4 AI ⚪ · 5 Write ⚪ · 6 Security ✅.
- **الأرقام كلها محسوبة — شوفها على `/api/v1/map`**، مافيش رقم مكتوب بالإيد في أي واجهة.
  (وقت الكتابة: 22 نظام · 21 كنكتور · 58 endpoint · **183 متطلب** · 8 محرّكات ·
  107 جاهز للبناء.)
- **أرقام نما (as-of 14 يوليو):** 549 عميل · 586 مورد · مبيعات 329.9M · AR 20.0M · مشتريات 320.0M · AP 186.7M.

## الفلوس — إزاي شغّالة، وإيه اللي لحظي فعلًا
- **لحظي دلوقتي (نما REST السحابية):** الموظفين · العملاء · الفواتير كمستندات · البنوك ·
  و**التحصيلات والمدفوعات** (`ReceiptVoucher`/`PaymentVoucher`) بعد إصلاح الـ400.
- **مش لحظي:** أي **رصيد** (AR/AP/KPIs) — `app/integrations/finance/` بيقرأ SQL محلي
  (`localhost/NAMA_TEST/stlix_gw` · ODBC Driver 17) وده **نسخة مُرستَرة**.
- **ليه:** نما REST مابتدّيش أرصدة محسوبة، و**SQL السحابي مقفول** (1433/1434 timeout — متأكد).
- **إزاي تعرف عمرها:** أي رد مالي فيه `freshness.as_of` و`age_days` و`stale`. متعرضة على
  الهَب وعلى صفحة التقارير.
- **التحديث:** إما AR/AP لحظي من REST (قيد التحقيق) أو restore لأحدث `.bak` من Drive.
- أرصدة البنوك لسه مؤجّلة (GL مش مُرحّل بالكامل في النسخة).

## مفتوح / التالي
- **المهمة التالية:** `NEXT_STEP.md` (سكربت المطابقة → قلب الافتراضي). وبعدها أوضح اختيار:
  `/api/v1/ideas?readiness=ready` = **107 متطلب محتاجين تقرير بس**، مش تكامل.
- **أضعف عقدتين في الخريطة:** `housing` و`meals` شايلين متطلب واحد بس بينهم (H15).
  والتغذية دي **هدف مشروع البصمة** — يعني الأغلب إن الباكلوج ناقصه سطور، مش إن النظام زيادة.
- 🔴 **تغيير المفاتيح المكشوفة** (Anthropic أولًا) — مؤجّل بطلب المالك (`secrets/gates-keys.backup.md`).

## حزمة الـ Handover (الملفات)
- `MASTER_EXECUTION_RUNBOOK.md` — المرجع الرئيسي (ابدأ منه)
- `SESSION_STATE.json` — الحالة المُهيكلة (machine-readable)
- `HANDOFF.md` — الملف ده (حالة آخر جلسة)
- `TASKS.md` · `DECISIONS.md` · `CHANGELOG.md` · `NEXT_STEP.md`

## مراجع تانية في الريبو
`BACKLOG.md` (181 متطلب — مصدر الحقيقة) · **`docs/ideas-wiring.md`** (كل فكرة: أنظمتها،
endpoints داتاها، وسبب كل ربط) · `VISION.md` (6 محرّكات) · `RESOURCES.md` · `docs/` · `templates/` · `modules/`.
كل ده بقى مقروء من `/tools/library` كمان.
الذاكرة: `stlix-gateway-project` · `nama-rest-protocol` · `attendance-app-project`.
