# NEXT_STEP — المهمة التالية فقط

> ملف واحد، مهمة واحدة. لما تخلص، حدّثه بالمهمة اللي بعدها من `TASKS.md`.

## المهمة الحالية: **الأرقام اللحظية — نحسم مصدرها**

المالك طلب «connect to live version». اللي اتأكد لحد دلوقتي:

| | |
|---|---|
| نما REST | **حيّة فعلًا** — موظفين · عملاء · فواتير كمستندات · بنوك · CRM · جرد |
| كل رقم فلوس (AR · AP · KPIs) | من **نسخة مُرستَرة** — `as_of` بيظهر دلوقتي على كل رد |
| SQL مباشر على سحابة نماسوفت | **مقفول** — tcp/1433 و1434 timeout (متأكد منه، مش افتراض) |
| `D:\hardsteel20260715.bak` | أحدث بيوم واحد بس عن المُرستَر — مايستاهلش restore |

**المفاجأة:** الجيتواي كان بيرمي داتا حيّة وصلته. نما بترجّع **HTTP 400 على النجاح الجزئي**
(بعض السجلات فشلت والباقي جوّه الرد)، والكلاينت كان بيعتبرها خطأ. اتصلح، فبقى عندنا:
`ReceiptVoucher` (تحصيلات العملاء) · `PaymentVoucher` (مدفوعات الموردين) · `JournalEntry` —
كلهم **لحظيين** وفيهم الطرف والحساب والمبلغ لكل سطر.

### فيه فخّين اتأكدوا — أي حساب بيتجاهلهم هيطلع غلط
1. **`records_count` بتاع الصفحة، مش الإجمالي.** بيعدّ اللي اتسلسل بنجاح في الرد ده بس.
   الـ paging الحقيقي: `startPage` (بيبدأ من 1) + `pageSize` (لحد 1000).
2. **`@draft`** لاحقة بتحطها نما على كود المستند غير المُرحَّل. لازم يتفلتروا وإلا المسودّات
   هتتحسب مع المُرحَّل.

### الخطوة
1. اقرأ نتيجة التحقيق (الـ workflow اللي اسمه `live-money-from-nama-rest`) — بيقارن
   AR/AP محسوبة من REST بأرقام SQL لنفس الفترة، وبيقول تُشحن ولا لأ.
2. **لو طابقت:** ابنِ `live` mode في `app/integrations/finance/` — نفس الـ endpoints،
   بس `?source=live`، مع `freshness.live = true`. **ماتعرضش رقم فلوس قبل ما يطابق SQL.**
3. **لو ماطابقتش:** ارجع للخطة القديمة — سكربت ليلي ينزّل أحدث
   `hardsteel<YYYYMMDD>.bak` من Google Drive folder `1yvCI6unRWALtzf1yiU9kiHAPaBB56Xtt/full`
   ويعمل `RESTORE DATABASE NAMA_TEST ... WITH REPLACE`. النتيجة: فارق يوم بدل 11.
   (`sqlcmd` متثبّت؛ مافيش rclone/gdrive — محتاج credentials للحساب
   `nama.customers.backup@gmail.com`.)

**القاعدة اللي ماتتكسرش:** إن البيانات قديمة ده سيّئ؛ إن رقم فلوس يكون **غلط** ده أسوأ بكتير.

## بدائل جاهزة (لو المالك عايز حاجة تانية الأول)
- **107 متطلب كنكتورهم جاهز** — محتاجين تقرير بس: `/api/v1/ideas?readiness=ready`.
  أكبر تجمّع: نما 102 · CRM 12 · مخازن 6.
- **ربط حصر مساعد المهندس بنما** (PD2) — فحص التكرار موجود في `/api/v1/nama/invitem/exists`.
- **الصوت العربي + شات الفريق** (Layer 4 + Web Speech) — مش متوقف على حاجة.
- **أول محرّك: التنبيهات (Renewals)** — بقى ليه 9 بنود موسومة.

## المطلوب قبل ما تبدأ
- شغّل السيرفر: preview name `stlix-gateway` → افتح `http://localhost:8000/tools/platform`.
- اقرأ `MASTER_EXECUTION_RUNBOOK.md` (المرجع) + `HANDOFF.md` (الحالة).

## بند مؤجّل (مش عاجل بس مهم)
🔴 **تغيير المفاتيح المكشوفة** (Anthropic أولًا) — المالك أجّله. `secrets/gates-keys.backup.md`.
