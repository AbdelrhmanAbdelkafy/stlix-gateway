# NEXT_STEP — المهمة التالية فقط

> ملف واحد، مهمة واحدة. لما تخلص، حدّثه بالمهمة اللي بعدها من `TASKS.md`.

## المهمة الحالية: **شغّل المطابقة، وبعدين اقلب الافتراضي على live**

الأرقام اللحظية **اتحلّت**. نما REST مابتنشرش رصيد محسوب، لكنها بتنشر المستندات،
والرصيد بيتبني منها:

```
صافي الفاتورة  = SUM(details[].price.netValue)
المحصّل         = SUM(externalPaymentLines[].paymentValue)
المفتوح         = الصافي − المحصّل
```

الكود موجود وشغّال: `app/integrations/finance/live.py` + `?source=live`.
**الناقص خطوة واحدة بس: المطابقة تتشغّل على جهازك.**

### الخطوة (٣ أوامر)

```bash
# 1) المطابقة الصارمة: REST المحلي مقابل SQL — نفس الداتا، فمايصحّش يختلف أي رقم
.venv/Scripts/python.exe scripts/reconcile_live_vs_sql.py --rest local --period 202606

# 2) المطابقة الحقيقية: REST السحابي مقابل نسخة SQL — السداد بعد النسخة مسموح، غيره لأ
.venv/Scripts/python.exe scripts/reconcile_live_vs_sql.py --rest cloud --period 202606

# 3) لو الاتنين خرجوا "reconciled": في .env
FINANCE_DEFAULT_SOURCE=live
```

بعدها restart، وكل صفحة مالية بتبقى لحظية افتراضيًا (**قرار المالك، 2026-07-25**).
والرجوع نفس السطر — `sql` تاني، من غير أي تعديل كود.

**⚠️ قبل ما تقلبها:** الـ live لقطة بتتبني في الخلفية (~8 دقايق / ~14,000 مستند).
يعني بعد كل restart، أول ما تفتح صفحة مالية هتلاقي «مافيش لقطة لسه» لحد ما تعمل
`POST /api/v1/finance/live/refresh`. لو ده مش مقبول، الخطوة اللي بعدها في `TASKS.md`
هي **مسح تلقائي عند الإقلاع + تحديث دوري** — وده اللي بيخلّي `live` كافتراضي مريح فعلًا.

### ليه سكربت مش مجرد تأكيد
المقارنة اتعملت مرة بالإيد على يونيو 2026 (302 فاتورة: صفر فرق في الإجمالي،
1.49 ج تقريب في الصافي، وكل فرق في المحصّل مفسَّر). **تأكيد ماتقدرش تعيده = ادّعاء
مش ضابط.** السكربت بيرجّع exit code 1 لو فيه فرق واحد مش مفسَّر، فينفع يتحط في أي
فحص قبل النشر.

## اللي اتعمل في الجلسة دي
- `scripts/reconcile_live_vs_sql.py` — المطابقة بقت أمر تعيده وقت ما تحب.
  وضعين: `--rest local` (نفس الداتا → مايصحّش يختلف أي رقم) و`--rest cloud`
  (السداد بعد النسخة **مفسَّر**؛ أي حركة في الإجمالي أو الصافي، أو سداد بيرجع
  لورا = **مش مفسَّر** وبيوقف النشر).
- **`is_draft` كانت بتسرّب مستند:** لقينا في SQL فاتورة مشتريات `documentFileStatus='Draft'`
  **من غير كود خالص** — الفلتر بـ `@draft` لوحده كان بيعدّيها كأنها مُرحَّلة. قيمتها 0 ج،
  وعشان كده مكانش حد هيلاحظ. اتصلحت (مافيش ولا مستند مُرحَّل من الـ13,686 كوده فاضي).
- **`subsidiary.code` من REST بيرجع الكود الحالي مش القديم** — اتقاس: فاتورة `SV00001`
  في SQL كودها `C00002` (مش موجود في `Customer` أصلًا) وREST بترجّع `C2000001` الصح.
  يعني مسار الـ live **محصّن** من فخّ الـ226 كود القديم اللي كسر مسار SQL. ✅
- `hub.public.html` بقى يقرا من `/api/v1/map` واتوصّل على `/tools/platform/public`
  (كان يتيم وفيه AR/AP وتاريخ مكتوبين بالإيد).
- `FINANCE_DEFAULT_SOURCE` — مصدر الأرقام الافتراضي بقى إعداد مش ثابت في الكود.
- **مساعد المهندس اتسجّل كموديول مؤسسي** في `VISION.md` جنب Finance OS وNameBuilder.
- **105 اختبار يعدّوا** (الرَنبوك كان لسه بيقول 89؛ بقوا 102 قبل الجلسة دي).

## بدائل جاهزة (لو المالك عايز حاجة تانية الأول)
- **107 متطلب كنكتورهم جاهز** — محتاجين تقرير بس: `/api/v1/ideas?readiness=ready`.
  أكبر تجمّع: نما 102 · CRM 12 · مخازن 6.
- **ربط حصر مساعد المهندس بنما** (PD2) — فحص التكرار موجود في `/api/v1/nama/invitem/exists`.
- **الصوت العربي + شات الفريق** (Layer 4 + Web Speech) — مش متوقف على حاجة.
- **أول محرّك: التنبيهات (Renewals)** — بقى ليه 9 بنود موسومة.

## المطلوب قبل ما تبدأ
- شغّل السيرفر: preview name `stlix-gateway` → افتح `http://localhost:8000/tools/platform`.
- اقرأ `MASTER_EXECUTION_RUNBOOK.md` (المرجع) + `HANDOFF.md` (الحالة).

## بند مؤجّل (مش عاجل بس مهم)
🔴 **تغيير المفاتيح المكشوفة** (Anthropic أولًا) — المالك أكّد التأجيل (2026-07-25).
`secrets/gates-keys.backup.md`.
