# NEXT_STEP — المهمة التالية فقط

> ملف واحد، مهمة واحدة. لما تخلص، حدّثه بالمهمة اللي بعدها من `TASKS.md`.

## ✅ اتقفلت: الأرقام اللحظية من الإنتاج + حارس بيراقبها

| | |
|---|---|
| REST | `https://stlixvalley.namasoft.net/erp/rest/v1` — **الإنتاج** |
| `FINANCE_DEFAULT_SOURCE` | `live` |
| `LIVE_FINANCE_REFRESH_SECONDS` | `3600` — مسح خلفي كل ساعة، بيبدأ عند الإقلاع |
| المطابقة الأولى | **0.17 ج في AR · 0.16 ج في AP** على كل المجتمع |
| الحارس | بعد كل مسح، شهر مقفول بيتطابق تاني · `pass` / `drift` / `unknown` |
| الرجوع | `FINANCE_DEFAULT_SOURCE=sql` — سطر واحد |

**الحارس بيمسك إيه:** حقل في نما اتغيّر (الصافي يقرا أقل) · صلاحية الـ credential ضاقت
(مستندات تختفي) · مسودّة دخلت المُرحَّل (مستندات تزيد). التلاتة **مايرموش خطأ** — المسح
بينجح والرقم يطلع ناقص أو زايد والتوقيت شكله سليم.

**وبيسيب إيه:** زيادة المحصّل. فاتورة مايو بتتسدّد في يوليو، والنسخة المُرستَرة متأخرة في
السداد بحكم التعريف. الثابت في الشهر المقفول = **مجموعة المستندات + إجمالي وصافي كل مستند**،
مش المحصّل. لو رنّينا على السداد، الحارس كان هيرنّ كل ساعة والناس تتعلّم تتجاهله.

⚠️ **بعد كل restart:** ~8 دقايق الصفحات المالية تقول «مافيش لقطة لسه» — مش بترجع لـ SQL. مقصود.

---

## المهمة الحالية: **Finance OS — الشاشات الديمو تتوصّل ولا تتخفي**

من `TASKS.md` (🟡): «dashboard KPI strip + Customers/Suppliers حقيقي؛ **باقي شاشاته ديمو**
(AP invoices · GL ledger · dashboard cash) — يتربطوا أو يتخفوا».

### ليه دي المهمة الجاية تحديدًا
الكوكبيت بقى فيه **فلوس إنتاج حقيقية** جنب **شاشات أرقامها مخترعة**. ودي نفس فئة الباگ
اللي قضينا الجلسة كلها بنقتلها:

- `hub.public.html` كان فيه 20.0M و186.7M مكتوبين بالإيد
- صفحة التقارير كانت حاطة شارة «حيّ» على أرقام عمرها 11 يوم
- المسودّات كانت بتتجمّع مع المُرحَّل
- والحارس نفسه اتعمل عشان الرقم الناقص مايبانش سليم

كلهم **صفحة بتدّعي حاجة مش بتعملها**. شاشة GL ledger بأرقام ديمو جوّه كوكبيت الرئيس أخطر من
الأربعة، لأن الأرقام الحقيقية اللي جنبها بتخليها تبان حقيقية. وقاعدة المشروع مكتوبة أصلًا:
**«اللوحة ماتقدرش تدّعي شغل ماتعملش»**.

### الخطوة
1. اعدّ الشاشات الديمو في `modules/finance-os/` وحدّد لكل واحدة: فيه داتا خام تغذّيها
   دلوقتي ولا لأ؟ (`/api/v1/map` بيقول الـ endpoints المتاحة).
2. **اللي عنده داتا** → يتوصّل ويتوسم بمصدره وعمره زي أي رقم مالي تاني.
3. **اللي مالوش داتا** → يتخفي أو يبان **placeholder صريح** بيقول «مستني إيه»، زي ما
   الـ501 placeholders بتعمل في `/api/v1/<key>`.
4. **مافيش رقم مخترع يفضل معروض.** الخزينة اتعاملت كده صح قبل كده (شيلنا الأرقام الوهمية
   وقلنا «قيد الترحيل») — نفس المعيار.
5. اختبار يمنع رجوعها: أي رقم في الكوكبيت لازم يجي من endpoint حقيقي.

## بدائل جاهزة (لو المالك عايز حاجة تانية الأول)
- **أول محرّك: التنبيهات (Renewals)** — 9 بنود موسومة، أعلى رافعة في الباكلوج.
- **107 متطلب كنكتورهم جاهز** — محتاجين تقرير بس: `/api/v1/ideas?readiness=ready`.
- **ربط حصر مساعد المهندس بنما** (PD2) — `/api/v1/nama/invitem/exists` موجود.
- **الصوت العربي + شات الفريق** (Layer 4) — مش متوقف على حاجة.
- **`LG9` CargoX** — إنشاء مستندات الشحن والتحقق منها + شات بوت.
- **سكربت الـ backup الليلي** — يخلّي مرجع SQL بفارق يوم بدل 11، فالحارس يبقى أدقّ.

## المطلوب قبل ما تبدأ
- شغّل السيرفر: preview name `stlix-gateway` → `http://localhost:8000/tools/platform`.
- اقرأ `MASTER_EXECUTION_RUNBOOK.md` (المرجع) + `HANDOFF.md` (الحالة).
- ⚠️ **فيه فرع منفصل:** `live-numbers` في `D:\Nama Code project\stlix-gateway-live`
  (شات تاني كان شغّال على نفس الملفات). `main` لسه على `986d43b`.

## بند مؤجّل
🔴 **تغيير المفاتيح المكشوفة** (Anthropic أولًا) — المالك أكّد التأجيل (2026-07-25).
`secrets/gates-keys.backup.md`.
