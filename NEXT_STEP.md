# NEXT_STEP — المهمة التالية فقط

> ملف واحد، مهمة واحدة. لما تخلص، حدّثه بالمهمة اللي بعدها من `TASKS.md`.

## ✅ اتقفلت: الأرقام اللحظية من إنتاج نما

الافتراضي بقى **`live`** (قرار المالك، 2026-07-25) — يعني `/api/v1/finance/*` بترد من
مستندات **إنتاج نما** مباشرة لما المستدعي مايقولش `?source=`.

| | |
|---|---|
| REST | `https://stlixvalley.namasoft.net/erp/rest/v1` — **الإنتاج** |
| `FINANCE_DEFAULT_SOURCE` | `live` |
| `LIVE_FINANCE_REFRESH_SECONDS` | `3600` — مسح في الخلفية كل ساعة، بيبدأ عند الإقلاع |
| المطابقة | **0.17 ج في AR · 0.16 ج في AP** على كل المجتمع، بعد تفسير الحركة بعد النسخة |
| SQL المُرستَر | فضل المرجع الكامل على `?source=sql` |

**الرجوع سطر واحد:** `FINANCE_DEFAULT_SOURCE=sql` في `.env` + restart.

⚠️ **اللي لازم تتوقعه بعد كل restart:** المسح الكامل ~14,000 مستند / ~8 دقايق. في الفترة دي
الصفحات المالية بتقول **«مافيش لقطة لسه»** — **مش** بترجع لـ SQL. ده مقصود وباختيارك: رقم
مفقود واضح أحسن من رقم مصدره مش باين. لو غيّرت رأيك، الرجوع التلقائي حاجة صغيرة تتضاف.

---

## المهمة الحالية: **حارس الانحراف (drift guard)**

من `TASKS.md` (🟡): «الطريقة اتثبتت واتشحنت؛ الناقص مقارنة أوتوماتيك على نافذة مقفولة
وشارة `pass|drift` تمنع تغيير schema/صلاحية مستقبلي من نشر رقم غلط.»

### المشكلة اللي بيحلّها
المطابقة دلوقتي **حاجة بتشغّلها بإيدك**. الأرقام بقت لحظية من الإنتاج، والمسح بيتكرّر كل
ساعة **من غير ما حد يتحقق منه تاني**. فيه تلات حاجات ممكن تتغيّر تحتنا بصمت:
- **نما تغيّر شكل حقل** (زي ما `maxRecords` كانت بتتجاهل، و`fiscalPeriod` مش عمود أصلًا).
- **صلاحية الـ credential تضيق** → مستندات تبقى `not accessible` وتقع من المجموع.
- **قاعدة ترحيل جديدة** → مسودّات بعلامة مختلفة (زي المستند اللي مالوش كود).

في التلاتة، الرقم **يفضل بيتعرض** وهو ناقص. عمر البيانات ظاهر، بس **صحّتها** مش ظاهرة.

### الشكل المقترح
1. **نافذة مقفولة** — شهر خلص وماحصلش فيه حركة جديدة (مثلاً الشهر اللي قبل اللي فات).
   فيه المطابقة لازم تطلع **صفر** فرق غير مفسَّر دايمًا، فأي فرق = انحراف حقيقي مش توقيت.
2. **يشتغل مع المسح** — بعد كل sweep، `compare()` على النافذة دي (رخيصة: شهر واحد).
3. **الشارة تسافر مع الرقم** — `freshness.guard = "pass" | "drift"` + آخر مرة اتحقق، تظهر
   على الهَب وصفحة التقارير جنب `as_of`.
4. **الانحراف يوقف النشر مش يخفي الرقم** — `drift` بيقلب المصدر لـ`sql` تلقائيًا **وبيقول
   إنه عمل كده وليه**. الرقم الكامل القديم أحسن من الرقم اللحظي الناقص.
5. اختبارات على `pass` و`drift` والانقلاب نفسه.

### اللي موجود بالفعل ونبني عليه
- `scripts/reconcile_live_vs_sql.py` — `compare()` و`period_bounds()` و`_keyed()` كلهم
  قابلين للاستدعاء من جوّه التطبيق (`tests/test_finance_reconcile.py` بيستوردهم فعلًا).
- `refresh_loop()` في `live.py` — المكان الطبيعي للفحص بعد كل مسح.
- `freshness()` — المكان الطبيعي للشارة.
- `--allow-reversal` — لازم يبقى قائمة في الإعدادات مش وسيط سطر أوامر، عشان الحارس
  الأوتوماتيك يعرف الإلغاءات المراجَعة (`FP22026060000105` أولها).

## بدائل جاهزة (لو المالك عايز حاجة تانية الأول)
- **107 متطلب كنكتورهم جاهز** — محتاجين تقرير بس: `/api/v1/ideas?readiness=ready`.
- **ربط حصر مساعد المهندس بنما** (PD2) — `/api/v1/nama/invitem/exists` موجود.
- **الصوت العربي + شات الفريق** (Layer 4) — مش متوقف على حاجة.
- **أول محرّك: التنبيهات (Renewals)** — 9 بنود موسومة.
- **`LG9` CargoX** — إنشاء مستندات الشحن والتحقق منها + شات بوت (داتاها في نما موجودة).

## المطلوب قبل ما تبدأ
- شغّل السيرفر: preview name `stlix-gateway` → `http://localhost:8000/tools/platform`.
- اقرأ `MASTER_EXECUTION_RUNBOOK.md` (المرجع) + `HANDOFF.md` (الحالة).
- ⚠️ **فيه فرع منفصل:** `live-numbers` في `D:\Nama Code project\stlix-gateway-live`
  (شات تاني كان شغّال على نفس الملفات). `main` لسه على `986d43b`.

## بند مؤجّل
🔴 **تغيير المفاتيح المكشوفة** (Anthropic أولًا) — المالك أكّد التأجيل (2026-07-25).
`secrets/gates-keys.backup.md`.
