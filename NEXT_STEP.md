# NEXT_STEP — المهمة التالية فقط

> ملف واحد، مهمة واحدة. لما تخلص، حدّثه بالمهمة اللي بعدها من `TASKS.md`.

## المهمة الحالية: **حوّل مطابقة الأرقام اللحظية لحارس أوتوماتيك**

الحساب اللحظي نفسه خلص واتقاس على المجتمع الكامل:

- AR الحيّ = **19,914,391.24 ج**؛ الفرق غير المفسَّر عن نسخة SQL بعد عزل الحركات
  اللاحقة = **0.17 ج**.
- AP الحيّ = **187,491,246.17 ج**؛ الفرق غير المفسَّر = **0.16 ج**.
- مطابقة يونيو 2026 السحابية عدّت: 138 فاتورة مبيعات + 164 فاتورة مشتريات، بشرط
  تسجيل الإلغاء المعروف `FP22026060000105` صراحةً كـreviewed reversal.
- الوضع المحلي الصارم **ماعدّاش كتشغيل** لأن REST المحلي على `:8080` رفض بيانات الدخول
  السحابية بـ401. ده blocker إعداد credential محلي، مش فرق حسابي.

المصدر الافتراضي يفضل **`sql`**. `?source=live` متاح ومثبت، لكن مافيش قلب افتراضي
من غير قرار صريح من المالك وبعد وجود guard يوقف أي drift جديد.

## المطلوب

1. اعمل خدمة read-only للمطابقة على فترة مقفولة، تعيد حالة واضحة:
   `not_run | running | pass | drift | blocked`.
2. خزّن ملخص آخر تشغيل فقط: الفترة، وقت التشغيل، عدد المستندات، الفروق المفسرة وغير
   المفسرة، وأكواد أي مستندات محجوبة. ممنوع تخزين أسرار أو body كامل.
3. اربطها بـendpoint حالة GET آمن، وخلّي تشغيل المطابقة manual/admin فقط؛ مافيش كتابة
   في نما أو SQL.
4. لو الحالة `drift` أو `blocked`، المصدر الافتراضي مايتحولش لـlive، والواجهة تعرض السبب
   بدل ما تنشر رقم باعتباره موثوق.
5. اختبارات لازم تقفل:
   - duplicate invoice codes مايتدمجوش؛
   - HTTP 400 الجزئي يتحسب ناقصًا ويتوسم، مش يتعامل كفشل كامل؛
   - `@draft` والكود الفاضي يتشالوا؛
   - الإلغاء مايتعدّيش إلا بـallow-list صريح؛
   - أي unreadable/invalid document يمنع `pass`.

## أمر الإثبات الحالي

```powershell
.venv/Scripts/python.exe scripts/reconcile_live_vs_sql.py `
  --rest cloud `
  --period 202606 `
  --allow-reversal FP22026060000105
```

النتيجة المثبتة: `RESULT: reconciled`.

## بندان محتاجين قرار المالك — مايتحسموش تلقائيًا

- `modules/platform/hub.public.html`: يتشال، ولا يعتمد `/api/v1/map`؟ ملف من شات تاني
  موجود في الـworktree، لكن القرار **لسه مفتوح**.
- 🔴 تدوير المفاتيح المكشوفة (Anthropic أولًا): **مؤجّل بطلب المالك**.
  `secrets/gates-keys.backup.md`.
