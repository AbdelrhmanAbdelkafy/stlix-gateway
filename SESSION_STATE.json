{
  "project": "Stlix Gateway",
  "one_liner": "نقطة تكامل واحدة (FastAPI) تربط كل أنظمة الشركة — قراءة أولًا، مفاتيح server-side، بوابة مزدوجة.",
  "aka": "hub = gateway = platform = الهَب = الجيتواي = المنصّة — all the same project (owner's words). Not to be confused with 'D:\\CLAUDE SHARED MEMORY\\Enterprise Platform AI based' (unrelated doc set).",
  "path": "D:\\Nama Code project\\stlix-gateway",
  "git": "local only (no remote, by owner's choice)",
  "as_of": "2026-07-25",
  "language_pref": "Egyptian Arabic (reply to owner in it)",
  "run": {
    "how": ".claude/launch.json name 'stlix-gateway' -> uvicorn app.main:app on 127.0.0.1:8000 (no --reload)",
    "restart_after": "python code / registry / config changes (HTML/JS in modules/ is read fresh per request)",
    "venv": ".venv (Python 3.14; deps need current pins)",
    "tests": "89 passing (.venv/Scripts/python.exe -m pytest -q)"
  },
  "architecture_6_layers": {
    "1_gateway": "done",
    "2_connectors": "9 live of 21 catalogued (nama, sql, attendance, crm, banks, inventory + gateway/front-end/all)",
    "3_unified_workspace": "done — 8 sections incl. finance (SQL)",
    "4_ai_orchestrator": "planned (human-like chat, sales AI, collector AI)",
    "5_workflows_write": "planned (item create, invoice approve — audited + HITL)",
    "6_logs_monitoring_security": "done"
  },
  "connectors_live": [
    "nama",
    "sql",
    "attendance",
    "crm",
    "banks",
    "inventory",
    "gateway",
    "front-end",
    "all-connectors"
  ],
  "finance_sql": {
    "status": "LIVE — real balances from Nama SQL (REST exposes none)",
    "login": "stlix_gw (read-only, db_datareader)",
    "conn": "localhost / NAMA_TEST / ODBC Driver 17 / Encrypt=yes;TrustServerCertificate=yes",
    "env_keys": [
      "NAMA_SQL_SERVER",
      "NAMA_SQL_DATABASE",
      "NAMA_SQL_USER",
      "NAMA_SQL_PASSWORD",
      "NAMA_SQL_DRIVER"
    ],
    "driver_pkg": "pyodbc",
    "endpoints": [
      "/api/v1/finance/kpis",
      "/api/v1/finance/customers",
      "/api/v1/finance/suppliers"
    ],
    "real_figures_asOf_2026-07-14": {
      "customers": 549,
      "suppliers": 586,
      "salesTotal": 329913390,
      "arOutstanding": 20022676,
      "purchaseTotal": 320048231,
      "apOutstanding": 186710932
    },
    "data_quality_finding": "AR 20.0M but only ~178K attributed to named customers; rest on unlinked invoices — needs review.",
    "freshness": "as-of = latest restored .bak (2026-07-14). Newer daily backups (hardsteel<date>.bak, ~5.7GB) on Google Drive folder 1yvCI6unRWALtzf1yiU9kiHAPaBB56Xtt. Refresh = download+RESTORE (can't read .bak in place). Automate as nightly job."
  },
  "pages_served": {
    "/tools/platform": "unified hub (entry point) — live modules + placeholders + SSO placeholder",
    "/tools/finance-os": "role-based cockpit (Demo prototype). REAL: dashboard KPI strip, Customers 360 (customer AR + supplier AP). Treasury = honest (no fake numbers, GL balances pending). AP invoices / GL ledger = still demo.",
    "/tools/finance-reports": "100% REAL finance reports (AR/AP/KPIs) from SQL — the production finance UI",
    "/tools/name-builder": "item creation — reads Nama lists + local parser + fuzzy autocomplete + dedup (read-only; create = write step pending)",
    "/tools/ideas": "ideas board — all 181 owner requirements as links, parsed live from BACKLOG.md; search + filter by connector readiness; deep link /tools/ideas#<id>",
    "/api/v1/map": "THE unified graph — systems x connectors x endpoints x ideas x engines; every other view slices this same join so counts cannot disagree",
    "/systems/{key}": "one system: its connectors, endpoints and the requirements waiting on it",
    "/connectors/{key}": "one connector: what it reads and exactly which ideas it blocks",
    "/tools/library": "the repo's own markdown docs, readable from inside the platform",
    "/tools/engineer": "Engineer's assistant (PD2): tanks/vessels with ASME UG-27 shell thickness, sanitary piping, laser & forming, heat exchangers, BOQ. OFFLINE - holds no credentials, calls nothing; its BOM is copied by hand rather than checked against Nama's item master."
  },
  "ideas": {
    "source_of_truth": "BACKLOG.md (add a markdown row -> appears on the board, no code change)",
    "code": "app/ideas/{registry,wiring,router}.py + app/{catalog,graph}.py + modules/platform/ideas.html",
    "endpoints": [
      "/api/v1/ideas",
      "/api/v1/ideas/domains",
      "/api/v1/ideas/{id}"
    ],
    "counts_2026-07-25": {
      "total": 182,
      "domains": 26,
      "done": 2,
      "ready": 107,
      "partial": 44,
      "blocked": 29,
      "with_live_data": 135
    },
    "readiness_meaning": "ready = every connector it needs already exists, so it only needs a report built",
    "top_missing_connectors": {
      "new-system": 47,
      "external": 15,
      "ai-layer": 9
    },
    "wiring": "app/ideas/wiring.py places all 181: systems (registry.SYSTEMS keys), the live endpoints holding their RAW data, workspace section, and an Arabic 'what is missing'. Explicit per id; a new BACKLOG row inherits DOMAIN_DEFAULTS. See docs/ideas-wiring.md for the reasoning behind each.",
    "invariants_locked_by_tests": [
      "every idea lands on a real registry.SYSTEMS key",
      "every data endpoint is a real route (catalog.paths())",
      "a planned idea never links to a built page, and never gets one as a data source",
      "an idea with no data endpoints always carries an `unblock` link",
      "catalog and app routes match in BOTH directions (tests/test_catalog.py)"
    ]
  },
  "modules": {
    "finance-os": {
      "roles": [
        "PRESIDENT(default)",
        "CFO",
        "AP_MANAGER",
        "AP_CLERK",
        "OPERATOR",
        "AUDITOR"
      ],
      "principle": "ERP=system of record, AI never writes directly, audited workflow"
    },
    "name-builder": {
      "UX1": "fuzzy search + autocomplete + Arabic normalization done in type box; UX1b (full-name validator->dedup->reject) pending"
    }
  },
  "verified_nama_facts": [
    "REST is entity-CRUD ONLY (list/find/save/delete per entity via OpenAPI /erp/browseapi/openapi/{Entity}.json). NO SQL/report/balance op.",
    "Zero-result filtered list = HTTP 400 empty body (not 200+[]) — handled in NamaClient.find_first.",
    "textCriteria operator case-sensitive: 'Equal' only. Dates DD-MM-YYYY.",
    "Nama web SQL utility (/erp/vue.html#/utils/sql) + API browser are web-login features, NOT reachable with API creds.",
    "Real balances only via SQL (now wired) or a Namasoft-published report (spec at docs/nama-balance-report-spec.md, optional)."
  ],
  "open_blocker": "🔴 Rotate exposed keys (Anthropic first, then Nama/Vtiger/inventory) — DEFERRED by owner. See secrets/gates-keys.backup.md.",
  "next_step_file": "NEXT_STEP.md",
  "handover_files": [
    "SESSION_STATE.json",
    "MASTER_EXECUTION_RUNBOOK.md",
    "HANDOFF.md",
    "TASKS.md",
    "DECISIONS.md",
    "CHANGELOG.md",
    "NEXT_STEP.md"
  ],
  "memories": [
    "stlix-gateway-project",
    "nama-rest-protocol",
    "attendance-app-project"
  ],
  "graph": {
    "module": "app/graph.py",
    "why": "four maps existed and never met; this is the join they were missing",
    "counts": {
      "systems": 22,
      "connectors": 21,
      "endpoints": 57,
      "ideas": 182
    },
    "systems_without_ideas": []
  },
  "auth": {
    "header": "X-API-Key (apps, scripts, AI clients)",
    "cookie": "sg_key — set when serving a /tools/* page, HttpOnly + SameSite=lax, accepted for GET/HEAD ONLY so ambient credentials can never write. Without it every <a> into /api/v1/* 401s once a key is configured."
  },
  "what_is_actually_live": {
    "live_now_via_nama_rest": [
      "employees",
      "customers",
      "invoices as DOCUMENTS",
      "banks",
      "CRM (Vtiger)",
      "stocktake",
      "ReceiptVoucher (customer collections)",
      "PaymentVoucher (supplier payments)",
      "JournalEntry"
    ],
    "not_live": "every computed BALANCE (AR/AP/KPIs) - served from a restored backup",
    "why": "Nama REST exposes no computed balances, AND direct SQL to the Namasoft cloud tenant is unreachable: tcp/1433 and 1434 to stlixvalley.namasoft.net both time out (measured, not assumed).",
    "freshness_is_visible": "every finance response carries freshness{as_of, age_days, live, stale}; shown on the hub and /tools/finance-reports, which previously badged 11-day-old figures as 'حيّ'.",
    "local_backups_on_disk": "D:/hardsteel20260715.bak - only ONE day newer than what is restored (2026-07-14). Not worth a restore."
  },
  "nama_400_semantics": {
    "finding": "Nama answers HTTP 400 for TWO different things and the client conflated them.",
    "empty_400": "a valid filter matching zero rows -> empty body -> 'no match' (find_first).",
    "partial_400": "SOME records failed to serialise; the successful ones ARE in the body. This was being raised as an error, which is why ReceiptVoucher and PaymentVoucher looked unreadable. Now kept and tagged `_partial` so a sum built from an incomplete set can never look like a true total.",
    "traps_for_any_live_aggregate": [
      "records_count is PER PAGE, not a total - it counts only rows serialised in that response",
      "paging is startPage (1-based) + pageSize (max 1000)",
      "'@draft' suffix on a document code = UNPOSTED draft; must be filtered or drafts get summed with posted documents"
    ],
    "open_question": "can AR/AP be computed live from ReceiptVoucher + PaymentVoucher + invoices, and does it reconcile with SQL for the same period? Under investigation - no money figure ships before it matches."
  }
}
